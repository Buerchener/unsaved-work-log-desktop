# 提示音与交互检查 · 2026-09-20

微信消息音已接入本地 MP3。默认关闭，可在首次进入提示或任务栏右下角快速设置开启/试听。开启后实际收到后台消息会播放；前台正在看的会话不重复弹通知。刷新不会重放历史通知，恢复声音设置后需要一次用户点击解锁浏览器音频。此次检测了媒体 playing 事件、播放进度与暂停状态，没有将自动播放限制误判为资源缺失。

## 本次发现并修复

1. 提前结束工作后，已勾选但未提交的文档仍显示可用提交按钮。结算保护原本有效，但点击没有反馈。现在下班后的按钮及核对项禁用，并明确提示未完成工作保留到下次处理。
2. 已提交报告仍能修改核对项，随后访问不存在的提交按钮而报错。现在已完成文件为只读，控件更新也检查按钮是否存在。
3. 恢复进度的引导卡片会覆盖音乐播放按钮。现在主动打开或聚焦任意应用后收起引导，不改变消息、剧情时间或数值。

产品代码只修改 desktop.js；没有更改数值规则、剧情分支或存档格式。

## 实际点击验证

- 完成四项工作 → 答应打球 → 实际赴约 → 周二。
- 完成工作 → 加班 → 周二；答应朋友后分别选择提前告知、未告知，后果有区别。
- 未完成任何工作 → 提前离开 → 在家休息 → 周二；未完成事项仍未完成。
- 暂缓回复 → 拒绝 → 离开时改为赴约 → 周二。
- 新用户开场提示及主管通知；17:20 恢复提示；重新开始后取消保留原进度。
- 普通工作与加班双击提交均只结算一次；刷新不重复加分；加班后没有再次赴约入口。
- 保存日志、阅读、回复、切换应用不结算；日志仅包含完成的工作；隐藏指标不出现在玩家文本。
- 未读通知不抢焦点；关闭通知仍保留未读；通知中心跳到对应会话；切到其他联系人不会误读；悬停与键盘焦点暂停消失。
- 窗口拖动、缩放、最大化还原、最小化、关闭重开；勾选和私人草稿刷新后恢复。
- 音乐、相册、游戏、便笺操作；390px、1280px、1536px 视口没有严重横向溢出。
- 本地 file:// 运行；v1/v3 旧存档迁移，旧数据原文和绩效保留。

上述使用独立 Chromium 浏览器上下文，不重置用户的实际存档。修复后的回归没有捕获页面 JS 异常。声音验证确认浏览器成功播放，不代表已测每台设备的扬声器、静音开关或系统音量。

## 边界与证据

未验证 Safari、Firefox、实体手机和系统静音组合，不能承诺所有设备零问题。通知短暂覆盖桌面右下角时可关闭，再操作下方按钮。

工作区 output/playwright/v04/ 中保留 audit-suite-results.txt、audit-invariants-results.txt、audit-guide-regression-results.txt 和修复前复现记录。audit-client、audit-guide-client 与 audit-night-document.png 为实运行截图。screenshots/windows11/ 中保存此次主流程与小屏截图。
