# 《未保存的工作日志》第一幕 v0.4

本规范替代 v0.3 的固定桌面外壳规定。范围仅为周一早晨到周二入口；保留已有故事与数值规则。

## 电脑与应用

最外层是有湖边壁纸的个人电脑。首次进入没有任何应用打开；只有九个小型快捷方式、底部任务栏和时钟。办公绩效只在协作空间中显示。个人日历、私人聊天、相册、音乐、游戏和便笺均不属于公司平台。

桌面单击选中、双击或 Enter 打开；触屏单击打开。任务栏单击打开、恢复或最小化当前窗口。右上角统一使用最小化、最大化／还原、关闭按钮。打开通知和文件不会遮罩桌面或替换其他应用。

窗口可叠放、拖动、右下角缩放（也支持方向键），保持标题栏可达。办公默认 980×660，聊天 660×570，文档 860×630。最大化只覆盖任务栏以上区域，还原恢复原几何位置。小于 700px 时只展示前景窗口，仍以任务栏切换。

## 视觉

原创湖边插画作为壁纸，使用低饱和湖绿、暖黄与山灰。快捷方式使用彩色圆角图标，禁止大型网页入口卡片覆盖桌面。任务栏半透明，窗口以标题栏、边框和阴影区分层级。

办公采用清晰的蓝灰排版；私人聊天使用浅绿会话区域；游戏使用紫灰封面和翻牌；音乐使用暖粉、唱片和播放条；便笺采用淡黄纸面。系统字体，本地图片与音频，无远程字体、账号和 API。

`desktop.css` 负责电脑、窗口与个人软件；重新编写的 `office.css` 将业务样式限制在 `.app-surface` 以及必要的 `#modal-root` 确认层。不能重新加入固定办公侧栏、常驻个人看板或全局绩效卡片。

## 叙事事件

1. 周一 09:00 干净桌面。约 1.4 秒展示一次主管消息；不会自动打开应用。
2. 点击工作通知／协作空间，打开报告附件。文档独立显示批注、两项核对和提交。提交后保留文档，消息回执引出邮件。
3. 邮件发送、归档核对、会议纪要确认保留区别。四项合计 20 分，分别为 5、3、5、7；正常工作不改变隐藏生活指标。
4. 会议确认后 17:20，阿远邀约先展示，主管汇总请求稍后展示。原窗口和输入焦点保持不变。通知点击才打开对应聊天。
5. 对话回复仅记录意图。加班提交前显示预计 19:40 和私人安排冲突，并允许先联系朋友；真实提交才结算 +8 / -3。赴约或休息通过电脑菜单确认，实际行动 +4。
6. 工作日志只保存工作；关窗口不离开电脑；离开电脑确认实际行动；当晚有结果后合上电脑进入周二。未完成常规任务也可以离开。
7. 周二回到桌面；消息、个人日历结果、工作日志保留，当日绩效 0 / 25。不开放第二幕。

## 消息与通知

消息先写入本地存档，以稳定 ID 去重。通知记录与消息事实分开；每条记录保存渠道、时间、是否展示、是否收起，消息保存已读标记。

右下角最多两条横幅，宽 360px，其余排队。约 8 秒收起，鼠标悬停或键盘聚焦暂停计时。动画时间不影响剧情。关闭横幅不等于读过或拒绝。未读在图标与通知中心保留；实际前景会话中可见的消息才标记已读，其他联系人不清除阿远未读。已读的横幅自动收起。

对应会话前景时不重复弹横幅；后台、最小化或其他联系人前景仍提示。刷新不重放已展示历史。音效默认关，主动开启后才使用轻声合成提示。无浏览器通知权限请求。

## 辅助应用

文件管理器仅浏览虚构工作资料与个人收藏，链接到已有文档、相册和便笺。游戏库有可玩的无计时翻牌，以及两款收藏的详情与存档文字。音乐有两段原创 16 秒本地 WAV，可播放、暂停；不自动播放。相册三幅原创 SVG，可切换与放大。便笺可编辑保存，未保存草稿也随浏览器进度保留。

这些操作不改变故事时间或指标。所有窗口关闭和最小化均不自动结算行动。

## 存档和范围

v0.4 使用独立 localStorage 键，优先读取本版，再兼容 v0.3 与 v1，旧键保留不覆盖。旧进度迁移为桌面入口，数值不重算，详情在帮助中。不能读取的旧档保留，临时体验不覆盖它。原始 ZIP 和 v0.3 回退 ZIP 均保留。

隐藏生活指标仅在代码和开发验收中存在，正式界面不显示。不得添加故障惊吓、主题结论、道德评分、结局评级或第二幕循环。

## v0.4 Windows 11 外观修订（优先于前述桌面视觉）

按用户明确要求，电脑外层统一为 Windows 11 浅色风格，使用 web-design 流程更新现有规范。桌面不再使用自绘湖边壁纸或品牌式角落文字。

1. 氛围：Windows 11 个人电脑；Bloom 默认背景，居中任务栏、开始菜单、Fluent 彩色图标。
2. 色彩：强调色 #0067c0；任务栏 #f3f5fb；标题栏 #f3f3f3；正文 #202020；分隔 #e5e5e5。
3. 字体：Segoe UI / Microsoft YaHei UI / Microsoft YaHei / PingFang SC，本地字体；系统标签 12px、菜单 12–13px，设置标题 23px。
4. 组件：48px 任务栏；32px 标题栏；46px 宽右上角控件；8px 窗口圆角；4px 菜单按钮圆角；关闭悬停红色。
5. 布局：桌面左侧 82×90 小图标网格；底部中央开始、搜索与应用；右侧语言、快捷设置、时钟和通知；最右显示桌面。
6. 层级：应用按焦点叠放，开始／通知面板在任务栏之上；确认框仅用于叙事行动；不伪装真实操作系统权限。
7. 交互：开始搜索过滤应用；电源菜单提供离开电脑；桌面右键提供个性化、刷新与帮助；设置→个性化→背景可切换三张本地官方背景，选择随进度保存。减少动效继续生效。
8. 护栏：不显示网页式欢迎卡片；不混用 macOS 红黄绿控件；不把绩效放到任务栏；不在桌面印品牌文案；不自动播放音频；不切换浏览器真实全屏；不申请系统权限；不修改真实电脑背景。叙事与指标保持原规则。
9. 响应：小于 1000px 任务栏应用组靠左避让托盘；小于 700px 前景单窗，任务栏可横向滚动，开始菜单四列。

相册三张图片由内置 imagegen 生成，为虚构生活照片；不再使用原手绘 SVG 相册图。默认桌面背景为 Microsoft 官方 Bloom 浅色，另含深色与 Surface Hub 蓝紫背景，来源见 assets/ASSET_SOURCES.md。
